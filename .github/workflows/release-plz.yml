name: Release-plz

permissions:
  pull-requests: write
  issues: write # needed for labels
  contents: write

on:
  pull_request:
    types: [ closed ]
    branches: [ main ]

jobs:
  # Release unpublished packages.
  release-plz-release:
    name: Release-plz release
    runs-on: ubuntu-latest
    timeout-minutes: 30
    outputs:
      releases: ${{ steps.release-plz.outputs.releases }}
      releases_created: ${{ steps.release-plz.outputs.releases_created }}
      tvc_tag: ${{ steps.tvc-tag.outputs.tag }}
    # This environment releases crates on crates.io, requires approval
    environment: release
    # only trigger this job if the push to main is from a merged PR with a "release" label
    if: >
      github.repository_owner == 'tkhq' &&
      github.event.pull_request.merged == true &&
      contains(github.event.pull_request.labels.*.name, 'release')
    permissions:
      contents: write
      # Required for trusted publishing, see https://release-plz.dev/docs/github/quickstart#2-set-the-cargo_registry_token-secret
      id-token: write
    steps:
      - name: git checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          # `fetch-depth: 0` is needed to clone all the git history, which is necessary to
          # release from the latest commit of the release PR.
          fetch-depth: 0
      - name: install Rust
        uses: dtolnay/rust-toolchain@v1
        with:
          toolchain: stable
          components: clippy,rustfmt
      - name: Run release-plz
        id: release-plz
        uses: release-plz/action@f708778669256143d984cce4b23592637532e040 # v0.5.127
        with:
          command: release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Determine tvc release tag
        id: tvc-tag
        if: steps.release-plz.outputs.releases_created == 'true'
        env:
          RELEASES: ${{ steps.release-plz.outputs.releases }}
        run: |
          set -euo pipefail
          # Robustly extract the single tvc tag: normalize to an array, select the
          # tvc package, take the first match, and emit empty (not "null") if absent.
          tag=$(jq -r 'if type == "array" then . else [.] end
                       | map(select(.package_name == "tvc"))
                       | first
                       | .tag // empty' <<< "${RELEASES}")
          echo "tag=${tag}" >> "${GITHUB_OUTPUT}"

  upload-tvc-binaries:
    name: Upload tvc binary (${{ matrix.target }})
    needs: release-plz-release
    runs-on: ${{ matrix.os }}
    timeout-minutes: 60
    if: >
      github.repository_owner == 'tkhq' &&
      needs.release-plz-release.outputs.tvc_tag != ''
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-unknown-linux-musl
            os: ubuntu-latest
            build-tool: cross
            rustflags: -C target-feature=+crt-static
          - target: aarch64-unknown-linux-musl
            os: ubuntu-latest
            build-tool: cross
            rustflags: -C target-feature=+crt-static
          - target: aarch64-apple-darwin
            os: macos-14
            build-tool: cargo
            rustflags: ""
          - target: x86_64-apple-darwin
            os: macos-14
            build-tool: cargo
            rustflags: ""
    env:
      RUSTFLAGS: ${{ matrix.rustflags }}
    steps:
      - name: git checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - name: install Rust
        uses: actions-rust-lang/setup-rust-toolchain@166cdcfd11aee3cb47222f9ddb555ce30ddb9659 # v1.17.0
        with:
          cache: false
          target: ${{ matrix.target }}
      - name: Install cross
        uses: taiki-e/install-action@07b4745e0c39a41822af610387492e3e53aa222b # v2.83.4
        with:
          tool: cross
        if: contains(matrix.target, '-musl')
      - name: Upload tvc binary
        uses: taiki-e/upload-rust-binary-action@f0d45ae91ee7b8ee928de7a9d04d893a08bcbec6 # v1.30.2
        with:
          bin: tvc
          package: tvc
          target: ${{ matrix.target }}
          build-tool: ${{ matrix.build-tool }}
          locked: true
          tar: all
          ref: refs/tags/${{ needs.release-plz-release.outputs.tvc_tag }}
          token: ${{ secrets.GITHUB_TOKEN }}

  # Create a PR with the new versions and changelog, preparing the next release.
  release-plz-pr:
    name: Release-plz PR
    runs-on: ubuntu-latest
    timeout-minutes: 30
    if: ${{ github.repository_owner == 'tkhq' }}
    permissions:
      contents: write
      pull-requests: write
    concurrency:
      group: release-plz-${{ github.ref }}
      cancel-in-progress: false
    steps:
      - name: git checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          # `fetch-depth: 0` is needed to clone all the git history, which is necessary to
          # determine the next version and build the changelog.
          fetch-depth: 0
      - name: install Rust
        uses: dtolnay/rust-toolchain@v1
        with:
          toolchain: stable
          components: clippy,rustfmt
      - name: Run release-plz
        uses: release-plz/action@f708778669256143d984cce4b23592637532e040 # v0.5.127
        with:
          command: release-pr
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}